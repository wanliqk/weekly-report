WEEKLY_REPORT_ENVIRONMENT=development
WEEKLY_REPORT_HOST=127.0.0.1
WEEKLY_REPORT_PORT=0
# Required outside the test environment (>=32 chars). Electron generates and
# injects this automatically when it launches the sidecar; set it yourself
# only when running `python -m app` standalone for manual debugging.
WEEKLY_REPORT_RUNTIME_SECRET=
# WEEKLY_REPORT_LOG_DIR=.local-data/logs
