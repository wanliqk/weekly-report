# 代码审查记录

## 2026-08-05 21:01

### 审查范围

- 阶段 4：`AUTH-01`、`AUTH-02`、`USER-01`、`FE-01`、`FE-02`、`QA-04` 当前工作树。
- 后端初始化、JWT/鉴权、用户管理与测试；Electron safeStorage/IPC；Vue 鉴权流程、路由、API client、用户管理界面与测试。

### 审查结论

主 Agent 自审通过，当前未发现未解决的 P0/P1 问题；实现已达到独立 Reviewer 可接手状态。根据项目全局 DoD，本记录不替代独立审查，阶段任务保持 `REVIEW`；本地实现提交按用户明确指令创建。

### 已发现并修复

1. P1：用户名长度原先在去除首尾空白前校验，纯空白边界可能绕过最小有效长度；已在 bootstrap 与用户创建 Schema 中增加去空白后的长度校验和测试。
2. P1：已登录用户访问根路由时可能停留在不带应用布局的占位页；已按 bootstrap/登录状态重定向到 `/daily`，由应用布局承载业务入口。
3. P2：用户管理敏感变更确认框取消时会产生未处理 Promise；已捕获取消分支并直接返回。

### 安全与架构核对

- 业务写路径遵循 `API → Service → Repository → Model/DB`，Router 未直接操作 ORM。
- 所有 `/api/v1` 请求仍受 runtime secret 保护；认证入口外的阶段 4 API 同时校验 JWT，admin 路由额外校验角色。
- JWT 强制 `sub`、`role`、`ver`、`iat`、`exp`、`jti`；用户禁用、角色变化、改密或密码重置会使旧 Token 失效。
- Token 由 Electron Main 使用 safeStorage 加密持久化；renderer 仅内存持有，未使用 `localStorage`/`sessionStorage`，safeStorage 不可用时无明文回退。
- 管理接口只返回账号元数据，不包含密码哈希、`token_version` 或他人报告正文；敏感模式扫描未发现凭据日志。
- JWT 签名密钥为数据目录首次启动随机生成的 256-bit 值，独占创建、重启复用、损坏拒绝使用，未硬编码或暴露给 renderer。

### 验证证据

- 后端：Ruff check/format、mypy strict（66 个源文件）、pytest（94 项）通过；`uv sync --frozen` 锁文件一致。
- 前端：ESLint、TypeScript/Vue typecheck、Vitest（12 文件 49 项）通过。
- 集成与构建：真实 sidecar 集成测试 2 项、生产 build、`git diff --check` 通过。
- 构建仅报告 `@vueuse/core` 的第三方 PURE 注释位置提示，不影响构建成功；renderer 主 JS 约 2.8 MB，已有 ISS-007 跟踪 Element Plus 全量引入风险。

### 待办

- 由独立 Reviewer 复核本阶段，确认未解决 P0/P1 为零。
- 独立审查通过后更新六项任务为 `DONE`，并在后续工作中同步上下文状态。

---

## 2026-08-05 21:25

### 阶段 4 独立审查完成

- 用户确认阶段 4 独立审查已经完成。
- `AUTH-01`、`AUTH-02`、`USER-01`、`FE-01`、`FE-02`、`QA-04` 无未解决 P0/P1，满足项目 DoD。
- 阶段 4 正式标记为 `DONE`；实现提交为 `1a50e75`。
- 后续工作进入阶段 5，不回写或混入阶段 4 提交。

---

## 2026-08-05 22:06

## 代码审查报告

### 审查目标

- 类型：本地改动。
- 范围：阶段 5 `TEMPLATE-01`、`SETTING-01`、`DAILY-01`、`DAILY-02`、`FE-03`、`FE-04`、`QA-05`；FastAPI Schema/API/Service/Repository、Vue 模板与日报页面、动态字段组件、测试和派生上下文文档。
- 审查时间：2026-08-05 22:06（Asia/Shanghai）。

### 审查输入证据

- Diff 范围：阶段 4 状态提交 `00e647f` 后的当前工作树；未查看或引用其他分支及既往 Git 历史实现。
- 上游约束：`docs/需求理解.md`、`docs/方案设计.md`、`ai-docs/{requirements,architecture,modules,database,api,coding-rule,decisions}.md`。
- 后端门禁：Ruff check/format 通过；strict mypy 检查 81 个源文件通过；pytest 117 项通过。
- 前端门禁：ESLint、TypeScript/Vue typecheck 通过；Vitest 14 个文件 54 项通过；生产 build 通过。
- 集成门禁：真实 sidecar 集成测试 2 项通过；`git diff --check` 通过。
- 未执行：依赖和锁文件未变化，因此本轮未重复执行 `npm ci` 与 `uv sync --frozen`。

### 问题清单

#### 🔴 严重问题

无未解决严重问题。

#### 🟡 建议修改

无未解决建议修改。

#### 🟢 可选优化

无本阶段新增的未解决优化项。Element Plus 全量引入导致 renderer 主 JS 约 2.85 MB，继续由既有 ISS-007 跟踪，不在阶段 5 扩大范围。

### 审查中已修复

1. 日报数字字段原先会接受 `NaN/Infinity`，非标准 JSON 请求可能写入无法正常响应的数值；已增加有限数判断和原始请求体回归测试，统一返回 42201。
2. 模板版本和日报时间原先依赖操作系统本地时区显示；已统一固定为 `Asia/Shanghai`，与个人设置契约一致。
3. 日报详情初版混入了阶段 8 `FE-07` 的企业微信占位入口；已移除该 UI 和 capability 页面调用，阶段 5 只交付 `SETTING-01` 后端能力 API。
4. 日报内容校验器最初使用不变的 `dict[str, object]` 参数，导致严格 mypy 无法接受已解析内容；已改用协变只读 `Mapping[str, object]`，不改变运行行为。

### 安全与架构核对

- 所有业务写操作均遵循 `API → Service → Repository → Model/DB`；Router 未直接操作 ORM，Electron/renderer 未访问 SQLite 或引入业务 IPC。
- 模板、设置、日报 API 均依赖当前登录用户；日报详情、列表和所有条件更新强制 `owner_id`，越权详情返回 404，不向 admin 提供他人正文入口。
- 模板版本只新增、不更新/删除；日报创建保存完整模板快照，保存与提交只按快照校验。
- 日报状态和版本在同一 SQL 条件更新中校验；同日创建由唯一约束兜底；自动归档在提交事务内原子完成。
- 新增代码未写入 `localStorage`/`sessionStorage`，未使用 `v-html`，未记录 Token、runtime secret、密码或报告正文，也未产生外部企业微信请求。

### 测试充分性

- 覆盖六类模板字段、核心字段、稳定键、不可变历史、设置读写及能力开关。
- 覆盖同日并发、重复创建、历史/未来/闰日、日期/状态查询、模板快照不漂移和 owner 隔离。
- 覆盖草稿必填/类型/非法字段/非有限数字、手工与自动归档、非法状态、陈旧版本覆盖阻断。
- 前端纯函数覆盖稳定键载荷、核心字段/选项校验、快照内容初始化、字段错误映射和固定时区；完整桌面主链路 Playwright 仍按阶段 9 `QA-09` 计划执行。

### 亮点

- Service 将领域校验、事务和状态机集中管理，Repository 保持薄层并在写条件中同时携带 owner/status/version。
- 前端在 40904 时保留用户输入，42201 错误可定位到动态字段；重复日期创建直接打开既有日报。
- 阶段边界清晰：没有提前实现导出、周报、设置页面、企业微信占位或备份 UI。

### 结论

- 结论：`Approved`（主 Agent 自审）。
- 严重问题：0 个。
- 建议修改：0 个。
- 可选优化：0 个新增未解决项。
- 下一步建议：本地实现提交按用户明确指令创建；随后交由独立 Reviewer 复核。通过并关闭问题后再将阶段 5 七项任务标记为 `DONE`；提交本身不改变 `REVIEW` 状态，也不授权进入阶段 6 或推送远端。

---

## 2026-08-05 22:32

### 阶段 5 独立审查完成

- 用户确认阶段 5 独立审查已经完成。
- `TEMPLATE-01`、`SETTING-01`、`DAILY-01`、`DAILY-02`、`FE-03`、`FE-04`、`QA-05` 无未解决 P0/P1，满足项目 DoD。
- 阶段 5 正式标记为 `DONE`；实现提交为 `1d965fe`。
- 后续工作进入阶段 6，从 `EXPORT-01` 开始；不回写或混入阶段 5 提交。

---

## 2026-08-12 11:37

### CR-20260812-01 项目列表类别与权重

#### 审查范围

- 后端 `ProjectListEntry`、日报校验、Excel 导出及 API/导出/企业微信 Mapper 测试。
- Electron/Vue 类型、默认值、空行过滤、编辑布局、日报/周报只读展示及对应 Vitest。
- 需求、方案设计、决策和 AI 上下文文档；仅审查当前 `v1` 工作树差异。

#### 🔴 严重问题

无。

#### 🟡 建议修改

无。

#### 🟢 可选优化

无本次新增未解决项。

#### 兼容、安全与架构核对

- 10 个子列的前后端字段顺序、默认值和 Excel 表头一致；只有 `project`/`content` 必填。
- 历史日报、日期归档快照和周报 JSON 均经过共享 Pydantic 类型解析，缺失 `category`/`weight` 时分别补“重要”/空字符串，不需要数据库迁移。
- 默认类别行只有在其余字段均空且类别未改动时才过滤；用户修改类别或权重不会被静默丢弃。
- 新增 Excel 单元格继续经过公式转义；企业微信 Mapper 仍只同步工作项目和工作步骤，没有扩大外发正文范围。
- 未引入 ORM 直连、业务 IPC、外部网络调用、敏感日志、`localStorage`、`v-html` 或新的权限边界。

#### 测试与门禁

- 后端：Ruff check、变更文件 format check、mypy（150 个源文件）、完整 pytest（505 项）通过。
- 前端：lint、typecheck、Vitest（27 文件 265 项）、生产 build 通过。
- 已覆盖 10 字段往返、旧两字段条目补默认并提交、非法新增字段类型、Excel 列顺序/公式防护、企业微信字段排除、默认行过滤和日报/周报展示。
- 全仓 Ruff format check 仍只命中未修改的 `export_style.py`（既有 `ISS-029`）；本机 `backend/.env` 对从 `backend/` 启动的 pytest 造成的空 runtime secret 失败属于已记录环境基线，仓库根隔离运行的 505 项全部通过。

#### 结论

- `Approved`（主 Agent 自审）。
- 严重问题：0；建议修改：0；可选优化：0。

---

## 2026-08-12 13:13

### CR-20260812-02 项目责任人默认当前用户

#### 审查目标

- 类型：当前 `v1` 本地改动。
- 范围：项目列表默认值、日报初始化/保存、动态字段组件、后端结构默认、相关测试与契约文档。

#### 审查输入证据

- 前端：lint、typecheck、Vitest（27 文件 265 项）、生产 build 全部通过。
- 后端：Ruff check、变更文件 format check、mypy（150 个源文件）、完整 pytest（505 项）全部通过。
- `git diff --check` 通过；仅有 Windows 工作区常规 LF→CRLF 提示。

#### 🔴 严重问题

无。

#### 🟡 建议修改

无。

#### 🟢 可选优化

无本次新增未解决项。

#### 正确性与兼容性核对

- 当前登录用户昵称只由 `DailyDetailView` 从 auth store 读取，并作为参数注入纯工具函数和子组件；工具层不依赖 Pinia 初始化时序。
- 首次默认行与点击“添加项目”两条路径都使用当前 `display_name`；保存前使用同一昵称比较整套默认值，未触碰行会被过滤。
- 已保存项目条目直接克隆原值，不覆盖已有 owner；历史 JSON 缺 owner 时后端仍补空字符串，不会把当前查看者写成历史责任人。
- 当前工作树原有的预计/实际完成时间“当日”、完成情况“已完成”默认值已纳入空行判定、Pydantic 兼容、Excel 导出断言和契约文档。
- 未改变鉴权、所有权、数据库结构、企业微信同步字段或日志边界。

#### 测试充分性

- 覆盖默认 owner 生成、完整默认行过滤、owner 被修改后条目保留、既有项目数组不覆盖以及历史精简条目结构补默认。
- Excel 回归覆盖新结构默认值在单项目、多项目、多来源、公式转义和跨日期场景中的输出。

#### 结论

- `Approved`（主 Agent 自审）。
- 严重问题：0；建议修改：0；可选优化：0。

---
